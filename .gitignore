# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/pv-signal-radar
/materialize-sqlite
coverage.txt
*.cdx.json

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# IDE & OS files
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# Local runtime state and generated scientific artifacts must not become source
# history. The research/input and output directories carry their own allowlists
# so their README and .gitignore files remain versioned.
data/
runtime/
research/renv/library/
research/.Rproj.user/
.env
.env.*
*.sqlite
*.sqlite-*
*.parquet
